================================================================================
CÓDIGO COMPLETO — PÁGINA DE VENDAS MENTORIA VOE
================================================================================

--------------------------------------------------------------------------------
ARQUIVO: index.html
--------------------------------------------------------------------------------
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <!-- TODO: Set the document title to the name of your application -->
    <title>Lovable App</title>
    <meta name="description" content="Lovable Generated Project">
    <meta name="author" content="Lovable" />

    <!-- TODO: Update og:title to match your application name -->
    
    
    <meta property="og:type" content="website" />
    <meta property="og:image" content="https://pub-bb2e103a32db4e198524a2e9ed8f35b4.r2.dev/6a35584e-2a56-488a-aec5-cd392e2bb0ab/id-preview-50e92082--83c63fe2-af58-4dfb-a111-8eb13ff69bd1.lovable.app-1773187844447.png">

    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:site" content="@Lovable" />
    <meta name="twitter:image" content="https://pub-bb2e103a32db4e198524a2e9ed8f35b4.r2.dev/6a35584e-2a56-488a-aec5-cd392e2bb0ab/id-preview-50e92082--83c63fe2-af58-4dfb-a111-8eb13ff69bd1.lovable.app-1773187844447.png">
    
  
  
  
  <meta property="og:title" content="Lovable App">
  <meta name="twitter:title" content="Lovable App">
  <meta property="og:description" content="Lovable Generated Project">
  <meta name="twitter:description" content="Lovable Generated Project">
</head>

  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>


--------------------------------------------------------------------------------
ARQUIVO: tailwind.config.ts
--------------------------------------------------------------------------------
import type { Config } from "tailwindcss";

export default {
  darkMode: ["class"],
  content: ["./pages/**/*.{ts,tsx}", "./components/**/*.{ts,tsx}", "./app/**/*.{ts,tsx}", "./src/**/*.{ts,tsx}"],
  prefix: "",
  theme: {
    container: {
      center: true,
      padding: "2rem",
      screens: {
        "2xl": "1400px",
      },
    },
    extend: {
      fontFamily: {
        display: ['Montserrat', 'sans-serif'],
        body: ['Inter', 'sans-serif'],
      },
      colors: {
        border: "hsl(var(--border))",
        input: "hsl(var(--input))",
        ring: "hsl(var(--ring))",
        background: "hsl(var(--background))",
        foreground: "hsl(var(--foreground))",
        primary: {
          DEFAULT: "hsl(var(--primary))",
          foreground: "hsl(var(--primary-foreground))",
        },
        secondary: {
          DEFAULT: "hsl(var(--secondary))",
          foreground: "hsl(var(--secondary-foreground))",
        },
        destructive: {
          DEFAULT: "hsl(var(--destructive))",
          foreground: "hsl(var(--destructive-foreground))",
        },
        muted: {
          DEFAULT: "hsl(var(--muted))",
          foreground: "hsl(var(--muted-foreground))",
        },
        accent: {
          DEFAULT: "hsl(var(--accent))",
          foreground: "hsl(var(--accent-foreground))",
        },
        popover: {
          DEFAULT: "hsl(var(--popover))",
          foreground: "hsl(var(--popover-foreground))",
        },
        card: {
          DEFAULT: "hsl(var(--card))",
          foreground: "hsl(var(--card-foreground))",
        },
        gold: {
          DEFAULT: "hsl(42 100% 50%)",
          glow: "hsl(var(--gold-glow))",
          dark: "hsl(var(--gold-dark))",
        },
        surface: {
          elevated: "hsl(var(--surface-elevated))",
          glass: "hsl(var(--surface-glass))",
        },
        sidebar: {
          DEFAULT: "hsl(var(--sidebar-background))",
          foreground: "hsl(var(--sidebar-foreground))",
          primary: "hsl(var(--sidebar-primary))",
          "primary-foreground": "hsl(var(--sidebar-primary-foreground))",
          accent: "hsl(var(--sidebar-accent))",
          "accent-foreground": "hsl(var(--sidebar-accent-foreground))",
          border: "hsl(var(--sidebar-border))",
          ring: "hsl(var(--sidebar-ring))",
        },
      },
      borderRadius: {
        lg: "var(--radius)",
        md: "calc(var(--radius) - 2px)",
        sm: "calc(var(--radius) - 4px)",
      },
      keyframes: {
        "accordion-down": {
          from: { height: "0" },
          to: { height: "var(--radix-accordion-content-height)" },
        },
        "accordion-up": {
          from: { height: "var(--radix-accordion-content-height)" },
          to: { height: "0" },
        },
        "fade-in": {
          "0%": { opacity: "0", transform: "translateY(20px)" },
          "100%": { opacity: "1", transform: "translateY(0)" },
        },
      },
      animation: {
        "accordion-down": "accordion-down 0.2s ease-out",
        "accordion-up": "accordion-up 0.2s ease-out",
        "fade-in": "fade-in 0.6s ease-out forwards",
      },
    },
  },
  plugins: [require("tailwindcss-animate")],
} satisfies Config;


--------------------------------------------------------------------------------
ARQUIVO: src/index.css
--------------------------------------------------------------------------------
@import url('https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700;800;900&family=Inter:wght@300;400;500;600&display=swap');

@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  :root {
    --background: 220 20% 6%;
    --foreground: 40 20% 95%;

    --card: 220 18% 10%;
    --card-foreground: 40 20% 95%;

    --popover: 220 18% 10%;
    --popover-foreground: 40 20% 95%;

    --primary: 42 100% 50%;
    --primary-foreground: 220 20% 6%;

    --secondary: 220 15% 15%;
    --secondary-foreground: 40 20% 90%;

    --muted: 220 15% 15%;
    --muted-foreground: 220 10% 55%;

    --accent: 42 80% 45%;
    --accent-foreground: 220 20% 6%;

    --destructive: 0 84% 60%;
    --destructive-foreground: 0 0% 100%;

    --border: 220 15% 18%;
    --input: 220 15% 18%;
    --ring: 42 100% 50%;

    --radius: 0.75rem;

    --gold-glow: 42 100% 60%;
    --gold-dark: 42 80% 35%;
    --surface-elevated: 220 18% 12%;
    --surface-glass: 220 20% 8%;

    --gradient-gold: linear-gradient(135deg, hsl(42 100% 50%), hsl(32 100% 45%));
    --gradient-dark: linear-gradient(180deg, hsl(220 20% 6%), hsl(220 25% 3%));
    --gradient-card: linear-gradient(135deg, hsl(220 18% 12%), hsl(220 20% 8%));

    --shadow-gold: 0 0 40px -10px hsl(42 100% 50% / 0.3);
    --shadow-card: 0 10px 40px -15px hsl(0 0% 0% / 0.5);

    --font-display: 'Montserrat', sans-serif;
    --font-body: 'Inter', sans-serif;

    --sidebar-background: 220 18% 10%;
    --sidebar-foreground: 40 20% 95%;
    --sidebar-primary: 42 100% 50%;
    --sidebar-primary-foreground: 220 20% 6%;
    --sidebar-accent: 220 15% 15%;
    --sidebar-accent-foreground: 40 20% 95%;
    --sidebar-border: 220 15% 18%;
    --sidebar-ring: 42 100% 50%;
  }
}

@layer base {
  * {
    @apply border-border;
  }

  body {
    @apply bg-background text-foreground;
    font-family: var(--font-body);
  }

  h1, h2, h3, h4, h5, h6 {
    font-family: var(--font-display);
  }
}

@layer components {
  .text-gradient-gold {
    @apply bg-clip-text text-transparent;
    background-image: var(--gradient-gold);
  }

  .bg-gradient-gold {
    background-image: var(--gradient-gold);
  }

  .bg-gradient-dark {
    background-image: var(--gradient-dark);
  }

  .bg-surface-elevated {
    background-color: hsl(var(--surface-elevated));
  }

  .bg-surface-glass {
    background-color: hsl(var(--surface-glass) / 0.8);
    backdrop-filter: blur(20px);
  }

  .shadow-gold {
    box-shadow: var(--shadow-gold);
  }

  .shadow-card {
    box-shadow: var(--shadow-card);
  }

  .btn-cta {
    @apply font-bold text-lg px-8 py-4 rounded-xl transition-all duration-300;
    font-family: var(--font-display);
    background-image: var(--gradient-gold);
    color: hsl(var(--primary-foreground));
    box-shadow: var(--shadow-gold);
  }

  .btn-cta:hover {
    transform: translateY(-2px);
    box-shadow: 0 0 60px -10px hsl(42 100% 50% / 0.5);
  }

  .pulse-dot {
    @apply absolute rounded-full;
    background: hsl(var(--primary));
    animation: pulse-glow 2s ease-in-out infinite;
  }

  .section-padding {
    @apply px-4 py-16 md:px-8 md:py-24 lg:py-32;
  }
}

@keyframes pulse-glow {
  0%, 100% {
    opacity: 1;
    transform: scale(1);
    box-shadow: 0 0 0 0 hsl(42 100% 50% / 0.7);
  }
  50% {
    opacity: 0.8;
    transform: scale(1.5);
    box-shadow: 0 0 10px 4px hsl(42 100% 50% / 0);
  }
}

@keyframes float {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-10px); }
}

.animate-float {
  animation: float 4s ease-in-out infinite;
}

/* Scrollbar */
::-webkit-scrollbar {
  width: 6px;
}
::-webkit-scrollbar-track {
  background: hsl(var(--background));
}
::-webkit-scrollbar-thumb {
  background: hsl(var(--border));
  border-radius: 3px;
}


--------------------------------------------------------------------------------
ARQUIVO: src/main.tsx
--------------------------------------------------------------------------------
import { createRoot } from "react-dom/client";
import App from "./App.tsx";
import "./index.css";

createRoot(document.getElementById("root")!).render(<App />);


--------------------------------------------------------------------------------
ARQUIVO: src/App.tsx
--------------------------------------------------------------------------------
import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
import { BrowserRouter, Route, Routes } from "react-router-dom";
import { Toaster as Sonner } from "@/components/ui/sonner";
import { Toaster } from "@/components/ui/toaster";
import { TooltipProvider } from "@/components/ui/tooltip";
import Index from "./pages/Index.tsx";
import NotFound from "./pages/NotFound.tsx";

const queryClient = new QueryClient();

const App = () => (
  <QueryClientProvider client={queryClient}>
    <TooltipProvider>
      <Toaster />
      <Sonner />
      <BrowserRouter>
        <Routes>
          <Route path="/" element={<Index />} />
          {/* ADD ALL CUSTOM ROUTES ABOVE THE CATCH-ALL "*" ROUTE */}
          <Route path="*" element={<NotFound />} />
        </Routes>
      </BrowserRouter>
    </TooltipProvider>
  </QueryClientProvider>
);

export default App;


--------------------------------------------------------------------------------
ARQUIVO: src/pages/Index.tsx
--------------------------------------------------------------------------------
import HeroSection from "@/components/HeroSection";
import GlobeSection from "@/components/GlobeSection";
import BenefitsSection from "@/components/BenefitsSection";
import BonusSection from "@/components/BonusSection";
import PricingSection from "@/components/PricingSection";
import GuaranteeSection from "@/components/GuaranteeSection";
import JourneySection from "@/components/JourneySection";
import ForWhoSection from "@/components/ForWhoSection";
import BioSection from "@/components/BioSection";
import FAQSection from "@/components/FAQSection";
import FooterSection from "@/components/FooterSection";
import FloatingCTA from "@/components/FloatingCTA";

const Index = () => {
  return (
    <div className="min-h-screen bg-background">
      <HeroSection />
      <GlobeSection />
      <BenefitsSection />
      <BonusSection />
      <JourneySection />
      <ForWhoSection />
      <PricingSection />
      <GuaranteeSection />
      <BioSection />
      <FAQSection />
      <FooterSection />
      <FloatingCTA />
    </div>
  );
};

export default Index;


--------------------------------------------------------------------------------
ARQUIVO: src/components/HeroSection.tsx
--------------------------------------------------------------------------------
import { motion } from "framer-motion";
import { Timer } from "lucide-react";

export default function HeroSection() {
  return (
    <section className="relative min-h-screen flex items-center justify-center bg-gradient-dark section-padding overflow-hidden">
      {/* Ambient glow */}
      <div className="absolute top-1/4 left-1/2 -translate-x-1/2 w-[600px] h-[400px] rounded-full opacity-20 blur-[120px]" style={{ background: "hsl(42 100% 50%)" }} />

      <div className="relative z-10 max-w-4xl mx-auto text-center">
        <motion.div
          initial={{ opacity: 0, y: 20 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.6 }}
          className="mb-6"
        >
          <span className="inline-flex items-center gap-2 bg-surface-elevated border border-border rounded-full px-5 py-2 text-sm font-display font-semibold text-primary">
            <Timer className="w-4 h-4" />
            1º LOTE LIBERADO COM 50% DE DESCONTO
          </span>
        </motion.div>

        <motion.h1
          initial={{ opacity: 0, y: 30 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.7, delay: 0.15 }}
          className="font-display font-black text-3xl md:text-5xl lg:text-6xl leading-tight mb-6"
        >
          <span className="text-foreground">GARANTA 5 ANOS DE ACESSO TOTAL</span>{" "}
          <span className="text-gradient-gold">AOS MEUS PRODUTOS, FORNECEDORES, PÁGINAS E ANÚNCIOS</span>{" "}
          <span className="text-foreground">PARA FATURAR</span>{" "}
          <span className="text-gradient-gold">+100 MIL TODOS OS MESES</span>
        </motion.h1>

        <motion.p
          initial={{ opacity: 0, y: 20 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.6, delay: 0.3 }}
          className="text-lg md:text-xl text-muted-foreground max-w-2xl mx-auto mb-8 font-body"
        >
          Pela primeira vez, você poderá garantir 5 anos de acesso ao único programa de acompanhamento do Brasil para você faturar +100 mil todos os meses (para sempre).
        </motion.p>

        <motion.div
          initial={{ opacity: 0, scale: 0.95 }}
          animate={{ opacity: 1, scale: 1 }}
          transition={{ duration: 0.5, delay: 0.45 }}
          className="flex flex-col items-center gap-3"
        >
          <a href="#pricing" className="btn-cta text-base md:text-lg">
            CLIQUE PARA GARANTIR O DESCONTO
          </a>
          <p className="text-sm text-muted-foreground">
            Lote Promocional com vagas limitadas que podem acabar sem aviso prévio
          </p>
        </motion.div>
      </div>
    </section>
  );
}


--------------------------------------------------------------------------------
ARQUIVO: src/components/Globe.tsx
--------------------------------------------------------------------------------
import { useRef, useMemo, useEffect } from "react";
import { Canvas, useFrame, useLoader, useThree } from "@react-three/fiber";
import { OrbitControls } from "@react-three/drei";
import * as THREE from "three";

function latLngToVector3(lat: number, lng: number, radius: number): [number, number, number] {
  const phi = (90 - lat) * (Math.PI / 180);
  const theta = (lng + 180) * (Math.PI / 180);
  const x = -(radius * Math.sin(phi) * Math.cos(theta));
  const z = radius * Math.sin(phi) * Math.sin(theta);
  const y = radius * Math.cos(phi);
  return [x, y, z];
}

const COUNTRIES: { name: string; lat: number; lng: number }[] = [
  { name: "México", lat: 23.6345, lng: -102.5528 },
  { name: "Guatemala", lat: 15.7835, lng: -90.2308 },
  { name: "Honduras", lat: 15.1999, lng: -86.2419 },
  { name: "El Salvador", lat: 13.7942, lng: -88.8965 },
  { name: "Nicarágua", lat: 12.8654, lng: -85.2072 },
  { name: "Costa Rica", lat: 9.7489, lng: -83.7534 },
  { name: "Panamá", lat: 8.538, lng: -80.7821 },
  { name: "Colômbia", lat: 4.5709, lng: -74.2973 },
  { name: "Venezuela", lat: 6.4238, lng: -66.5897 },
  { name: "Equador", lat: -1.8312, lng: -78.1834 },
  { name: "Peru", lat: -9.19, lng: -75.0152 },
  { name: "Bolívia", lat: -16.2902, lng: -63.5887 },
  { name: "Chile", lat: -35.6751, lng: -71.543 },
  { name: "Argentina", lat: -38.4161, lng: -63.6167 },
  { name: "Uruguai", lat: -32.5228, lng: -55.7658 },
  { name: "Paraguai", lat: -23.4425, lng: -58.4438 },
  { name: "Brasil", lat: -14.235, lng: -51.9253 },
  { name: "Cuba", lat: 21.5218, lng: -77.7812 },
  { name: "Rep. Dominicana", lat: 18.7357, lng: -70.1627 },
  { name: "Porto Rico", lat: 18.2208, lng: -66.5901 },
  { name: "Jamaica", lat: 18.1096, lng: -77.2975 },
  { name: "Trinidad e Tobago", lat: 10.6918, lng: -61.2225 },
  { name: "EUA", lat: 37.0902, lng: -95.7129 },
  { name: "Canadá", lat: 56.1304, lng: -106.3468 },
  { name: "Espanha", lat: 40.4637, lng: -3.7492 },
  { name: "Portugal", lat: 39.3999, lng: -8.2245 },
  { name: "Itália", lat: 41.8719, lng: 12.5674 },
  { name: "Alemanha", lat: 51.1657, lng: 10.4515 },
  { name: "França", lat: 46.2276, lng: 2.2137 },
  { name: "Reino Unido", lat: 55.3781, lng: -3.436 },
  { name: "Japão", lat: 36.2048, lng: 138.2529 },
  { name: "Austrália", lat: -25.2744, lng: 133.7751 },
  { name: "Coreia do Sul", lat: 35.9078, lng: 127.7669 },
  { name: "Índia", lat: 20.5937, lng: 78.9629 },
  { name: "Emirados Árabes", lat: 23.4241, lng: 53.8478 },
];

function createShopIconTexture(): THREE.CanvasTexture {
  const size = 128;
  const canvas = document.createElement("canvas");
  canvas.width = size;
  canvas.height = size;
  const ctx = canvas.getContext("2d")!;

  const gradient = ctx.createRadialGradient(size / 2, size / 2, 0, size / 2, size / 2, size / 2);
  gradient.addColorStop(0, "rgba(255, 184, 0, 0.9)");
  gradient.addColorStop(0.4, "rgba(255, 184, 0, 0.5)");
  gradient.addColorStop(1, "rgba(255, 184, 0, 0)");
  ctx.fillStyle = gradient;
  ctx.fillRect(0, 0, size, size);

  const bagW = 32;
  const bagH = 36;
  const bx = size / 2 - bagW / 2;
  const by = size / 2 - bagH / 2 + 4;
  ctx.fillStyle = "#FFB800";
  ctx.strokeStyle = "#1a1a2e";
  ctx.lineWidth = 2;
  ctx.beginPath();
  ctx.roundRect(bx, by + 10, bagW, bagH - 10, 4);
  ctx.fill();
  ctx.stroke();

  ctx.beginPath();
  ctx.arc(size / 2, by + 12, 10, Math.PI, 0, false);
  ctx.strokeStyle = "#1a1a2e";
  ctx.lineWidth = 3;
  ctx.stroke();

  const texture = new THREE.CanvasTexture(canvas);
  texture.needsUpdate = true;
  return texture;
}

function ShopMarker({ position }: { position: [number, number, number] }) {
  const spriteRef = useRef<THREE.Sprite>(null);
  const glowRef = useRef<THREE.Sprite>(null);
  const offset = useMemo(() => Math.random() * Math.PI * 2, []);
  const texture = useMemo(() => createShopIconTexture(), []);

  useFrame(({ clock }) => {
    const t = clock.getElapsedTime();
    const pulse = 1 + 0.3 * Math.sin(t * 2.5 + offset);
    if (spriteRef.current) {
      spriteRef.current.scale.setScalar(0.12 * pulse);
    }
    if (glowRef.current) {
      glowRef.current.scale.setScalar(0.22 * pulse);
      (glowRef.current.material as THREE.SpriteMaterial).opacity = 0.3 + 0.15 * Math.sin(t * 2.5 + offset);
    }
  });

  const glowTexture = useMemo(() => {
    const size = 64;
    const canvas = document.createElement("canvas");
    canvas.width = size;
    canvas.height = size;
    const ctx = canvas.getContext("2d")!;
    const g = ctx.createRadialGradient(size / 2, size / 2, 0, size / 2, size / 2, size / 2);
    g.addColorStop(0, "rgba(255, 184, 0, 0.6)");
    g.addColorStop(0.5, "rgba(255, 184, 0, 0.15)");
    g.addColorStop(1, "rgba(255, 184, 0, 0)");
    ctx.fillStyle = g;
    ctx.fillRect(0, 0, size, size);
    const tex = new THREE.CanvasTexture(canvas);
    tex.needsUpdate = true;
    return tex;
  }, []);

  return (
    <group position={position}>
      <sprite ref={glowRef} scale={[0.22, 0.22, 0.22]}>
        <spriteMaterial map={glowTexture} transparent depthWrite={false} blending={THREE.AdditiveBlending} />
      </sprite>
      <sprite ref={spriteRef} scale={[0.12, 0.12, 0.12]}>
        <spriteMaterial map={texture} transparent depthWrite={false} />
      </sprite>
    </group>
  );
}

const AtmosphereShader = {
  vertexShader: `
    varying vec3 vNormal;
    void main() {
      vNormal = normalize(normalMatrix * normal);
      gl_Position = projectionMatrix * modelViewMatrix * vec4(position, 1.0);
    }
  `,
  fragmentShader: `
    varying vec3 vNormal;
    void main() {
      float intensity = pow(0.65 - dot(vNormal, vec3(0.0, 0.0, 1.0)), 2.0);
      vec3 color = vec3(0.25, 0.55, 1.0);
      gl_FragColor = vec4(color, intensity * 0.45);
    }
  `,
};

function Atmosphere() {
  return (
    <mesh scale={[1.12, 1.12, 1.12]}>
      <sphereGeometry args={[1.5, 64, 64]} />
      <shaderMaterial
        vertexShader={AtmosphereShader.vertexShader}
        fragmentShader={AtmosphereShader.fragmentShader}
        transparent
        side={THREE.BackSide}
        depthWrite={false}
        blending={THREE.AdditiveBlending}
      />
    </mesh>
  );
}

function GlobeModel() {
  const globeRef = useRef<THREE.Group>(null);
  const earthTexture = useLoader(THREE.TextureLoader, "/textures/earth-night.jpg");
  const bumpTexture = useLoader(THREE.TextureLoader, "/textures/earth-topology.png");

  useEffect(() => {
    earthTexture.colorSpace = THREE.SRGBColorSpace;
  }, [earthTexture]);

  useFrame(({ clock }) => {
    if (globeRef.current) {
      globeRef.current.rotation.y = clock.getElapsedTime() * 0.08;
    }
  });

  const dots = useMemo(
    () => COUNTRIES.map((c) => ({ name: c.name, pos: latLngToVector3(c.lat, c.lng, 1.53) })),
    []
  );

  return (
    <group ref={globeRef}>
      <mesh>
        <sphereGeometry args={[1.5, 64, 64]} />
        <meshStandardMaterial
          map={earthTexture}
          bumpMap={bumpTexture}
          bumpScale={0.03}
          emissiveMap={earthTexture}
          emissive={new THREE.Color(0xffffff)}
          emissiveIntensity={0.8}
          roughness={0.85}
          metalness={0.1}
        />
      </mesh>
      <mesh>
        <sphereGeometry args={[1.505, 48, 48]} />
        <meshBasicMaterial color="#1a3a5c" wireframe transparent opacity={0.08} depthWrite={false} />
      </mesh>
      <Atmosphere />
      {dots.map((dot) => (
        <ShopMarker key={dot.name} position={dot.pos} />
      ))}
    </group>
  );
}

function SceneSetup() {
  const { gl } = useThree();
  useEffect(() => {
    gl.toneMapping = THREE.ACESFilmicToneMapping;
    gl.toneMappingExposure = 1.2;
  }, [gl]);
  return null;
}

export default function Globe() {
  return (
    <div className="w-full aspect-square max-h-[70vh]">
      <Canvas
        camera={{ position: [0, 0, 4.0], fov: 45 }}
        gl={{ antialias: true, alpha: true }}
        style={{ background: "transparent" }}
      >
        <SceneSetup />
        <ambientLight intensity={0.15} />
        <directionalLight position={[5, 3, 5]} intensity={0.4} color="#4488ff" />
        <pointLight position={[-10, -5, -10]} intensity={0.2} color="#FFB800" />
        <GlobeModel />
        <OrbitControls
          enableZoom={false}
          enablePan={false}
          autoRotate={false}
          minPolarAngle={Math.PI / 3}
          maxPolarAngle={Math.PI / 1.5}
          rotateSpeed={0.5}
        />
      </Canvas>
    </div>
  );
}


--------------------------------------------------------------------------------
ARQUIVO: src/components/GlobeSection.tsx
--------------------------------------------------------------------------------
import { Suspense, useRef } from "react";
import { motion, useScroll, useTransform } from "framer-motion";
import Globe from "./Globe";

export default function GlobeSection() {
  const sectionRef = useRef<HTMLDivElement>(null);

  const { scrollYProgress } = useScroll({
    target: sectionRef,
    offset: ["start start", "end start"],
  });

  // Headline shrinks and moves up as user scrolls
  const headlineScale = useTransform(scrollYProgress, [0, 0.5], [1, 0.7]);
  const headlineOpacity = useTransform(scrollYProgress, [0.4, 0.65], [1, 0.85]);
  const headlineY = useTransform(scrollYProgress, [0, 0.5], [0, -30]);

  // Globe shrinks as user scrolls toward CTA
  const globeScale = useTransform(scrollYProgress, [0, 0.55], [1, 0.55]);
  const globeY = useTransform(scrollYProgress, [0, 0.55], [0, -60]);

  // CTA fades in and rises
  const ctaOpacity = useTransform(scrollYProgress, [0.3, 0.55], [0, 1]);
  const ctaY = useTransform(scrollYProgress, [0.3, 0.55], [40, 0]);

  return (
    <section
      ref={sectionRef}
      className="relative bg-background overflow-visible"
      style={{ minHeight: "180vh" }}
    >
      <div className="sticky top-0 min-h-screen flex flex-col items-center justify-center px-4 py-8">
        {/* Headline */}
        <motion.div
          style={{ scale: headlineScale, opacity: headlineOpacity, y: headlineY }}
          className="text-center mb-2 z-10"
        >
          <motion.h2
            initial={{ opacity: 0, y: 20 }}
            whileInView={{ opacity: 1, y: 0 }}
            viewport={{ once: true }}
            transition={{ duration: 0.6 }}
            className="font-display font-black text-2xl md:text-4xl lg:text-5xl mb-2"
          >
            Venda no automático para{" "}
            <span className="text-gradient-gold">+32 países</span>{" "}
            com o Método VOE
          </motion.h2>
          <p className="text-muted-foreground text-sm md:text-base max-w-xl mx-auto">
            Toque e gire o globo para explorar
          </p>
        </motion.div>

        {/* Globe — starts large, shrinks on scroll */}
        <motion.div
          style={{ scale: globeScale, y: globeY }}
          className="w-full max-w-3xl z-0"
        >
          <Suspense
            fallback={
              <div className="w-full h-[50vh] md:h-[60vh] flex items-center justify-center">
                <div className="w-12 h-12 border-2 border-primary border-t-transparent rounded-full animate-spin" />
              </div>
            }
          >
            <Globe />
          </Suspense>
        </motion.div>

        {/* CTA appears as globe shrinks */}
        <motion.div
          style={{ opacity: ctaOpacity, y: ctaY }}
          className="z-10 mt-2"
        >
          <a href="#pricing" className="btn-cta text-base md:text-lg">
            COMPRAR AGORA
          </a>
        </motion.div>
      </div>
    </section>
  );
}


--------------------------------------------------------------------------------
ARQUIVO: src/components/BenefitsSection.tsx
--------------------------------------------------------------------------------
import { motion } from "framer-motion";
import { Video, Users, BookOpen, Store, Package } from "lucide-react";

const benefits = [
  { icon: Video, title: "Mentorias ao vivo para tirar dúvidas direto com o Gustavo Henrique" },
  { icon: Users, title: "Suporte e acompanhamento na Comunidade de Águias da VOE" },
  { icon: BookOpen, title: "Treinamento passo a passo para vender online em toda a América Latina" },
  { icon: Store, title: "Loja Online 100% Personalizada pelo Meu Time de Especialistas" },
  { icon: Package, title: "Acesso Premium a Plataforma de Fornecedores Latam" },
];

export default function BenefitsSection() {
  return (
    <section className="section-padding bg-surface-elevated">
      <div className="max-w-4xl mx-auto">
        <motion.h2
          initial={{ opacity: 0, y: 20 }}
          whileInView={{ opacity: 1, y: 0 }}
          viewport={{ once: true }}
          className="font-display font-black text-2xl md:text-4xl text-center mb-12"
        >
          O que você vai receber dentro da{" "}
          <span className="text-gradient-gold">Mentoria VOE?</span>
        </motion.h2>

        <div className="grid gap-4 md:gap-6">
          {benefits.map((b, i) => (
            <motion.div
              key={i}
              initial={{ opacity: 0, x: -30 }}
              whileInView={{ opacity: 1, x: 0 }}
              viewport={{ once: true }}
              transition={{ duration: 0.4, delay: i * 0.1 }}
              className="flex items-start gap-4 bg-card border border-border rounded-xl p-5 md:p-6 shadow-card"
            >
              <div className="flex-shrink-0 w-12 h-12 rounded-lg bg-gradient-gold flex items-center justify-center">
                <b.icon className="w-6 h-6 text-primary-foreground" />
              </div>
              <p className="font-display font-semibold text-base md:text-lg text-foreground leading-snug">{b.title}</p>
            </motion.div>
          ))}
        </div>
      </div>
    </section>
  );
}


--------------------------------------------------------------------------------
ARQUIVO: src/components/BonusSection.tsx
--------------------------------------------------------------------------------
import { motion } from "framer-motion";
import { Store, Package, TrendingUp, Wrench, GraduationCap, AlertTriangle } from "lucide-react";

const bonuses = [
  {
    icon: Store,
    title: "LOJA LATAM 100% PRONTA EM MENOS DE 3 DIAS",
    desc: "Você vai dizer como é a loja dos seus sonhos e o meu Time de Especialistas vai criá-la de forma profissional.",
    items: ["10 Produtos que já venderam +R$100 mil", "Páginas e categorias em espanhol", "Banners de Alta Qualidade", "Cores Personalizadas"],
    value: "R$ 1.500",
  },
  {
    icon: Package,
    title: "Acesso Exclusivo a Plataforma de Fornecedores Latam",
    desc: "Somos um dos únicos parceiros na América Latina, garantindo agilidade e eficiência na entrega.",
    items: ["Venda rápida e segura", "100% livre de taxas do governo", "Garantia do menor preço", "Entrega em menos de 4 dias"],
    value: "R$ 1.000",
  },
  {
    icon: TrendingUp,
    title: "Bastidores da Loja Milionária VOE",
    desc: "Acompanhe cada etapa de uma loja criada do zero até chegar aos R$100 mil/mês.",
    items: ["Escolha de produtos", "Criação de anúncios online", "Configuração de tráfego pago", "Estratégias para 3x lucro"],
    value: "R$ 1.000",
  },
  {
    icon: Wrench,
    title: "Caixa de Ferramentas Milionárias",
    desc: "Acesso a Esteiras de Produtos, Anúncios, Páginas de Vendas e Temas de Alta Conversão.",
    items: ["Planilha de Precificação", "Planilha de Mineração de Criativos", "Planilha de Mineração de Produtos", "Planilhas de Gestão"],
    value: "R$ 500",
  },
  {
    icon: GraduationCap,
    title: "Trilhas do Sucesso",
    desc: "Convidei profissionais de Gestão e Tráfego Pago para acelerar ainda mais os seus resultados.",
    items: ["Google Ads", "TikTok Ads", "Taboola", "Vendas no orgânico"],
    value: "R$ 800",
  },
];

export default function BonusSection() {
  return (
    <section className="section-padding bg-background">
      <div className="max-w-4xl mx-auto">
        <motion.div
          initial={{ opacity: 0, y: 20 }}
          whileInView={{ opacity: 1, y: 0 }}
          viewport={{ once: true }}
          className="text-center mb-12"
        >
          <span className="inline-flex items-center gap-2 bg-primary/10 border border-primary/30 rounded-full px-5 py-2 text-sm font-display font-bold text-primary mb-4">
            <AlertTriangle className="w-4 h-4" />
            BÔNUS LIMITADO — LOTE 1
          </span>
          <h2 className="font-display font-black text-2xl md:text-4xl">
            Bônus exclusivos que{" "}
            <span className="text-gradient-gold">podem acabar a qualquer momento</span>
          </h2>
        </motion.div>

        <div className="grid gap-5">
          {bonuses.map((bonus, i) => (
            <motion.div
              key={i}
              initial={{ opacity: 0, y: 20 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              transition={{ delay: i * 0.08 }}
              className="bg-card border border-border rounded-2xl p-6 md:p-8 shadow-card hover:border-primary/30 transition-colors"
            >
              <div className="flex items-start gap-4 mb-4">
                <div className="w-12 h-12 rounded-lg bg-gradient-gold flex items-center justify-center flex-shrink-0">
                  <bonus.icon className="w-6 h-6 text-primary-foreground" />
                </div>
                <div className="flex-1">
                  <h3 className="font-display font-bold text-base md:text-lg text-foreground">{bonus.title}</h3>
                  <p className="text-muted-foreground text-sm mt-1">{bonus.desc}</p>
                </div>
                <span className="text-primary font-display font-bold text-sm whitespace-nowrap">{bonus.value}</span>
              </div>
              <div className="grid grid-cols-2 gap-2 ml-16">
                {bonus.items.map((item, j) => (
                  <p key={j} className="text-sm text-muted-foreground flex items-center gap-2">
                    <span className="w-1.5 h-1.5 rounded-full bg-primary flex-shrink-0" />
                    {item}
                  </p>
                ))}
              </div>
            </motion.div>
          ))}
        </div>
      </div>
    </section>
  );
}


--------------------------------------------------------------------------------
ARQUIVO: src/components/JourneySection.tsx
--------------------------------------------------------------------------------
import { motion } from "framer-motion";
import { Globe, Store, Truck, Search, Megaphone, TrendingUp, Rocket } from "lucide-react";

const modules = [
  { icon: Globe, title: "MÓDULO 01", desc: "Os segredos do Mercado Latam" },
  { icon: Store, title: "MÓDULO 02", desc: "Como deixar sua loja 5 estrelas" },
  { icon: Truck, title: "MÓDULO 03", desc: "Escolhendo fornecedores de confiança" },
  { icon: Search, title: "MÓDULO 04", desc: "Encontrando produtos milionários" },
  { icon: Megaphone, title: "MÓDULO 05", desc: "Subindo os seus primeiros anúncios na prática" },
  { icon: TrendingUp, title: "MÓDULO 06", desc: "Os segredos do tráfego pago" },
  { icon: Rocket, title: "MÓDULO 07", desc: "Pós venda - escalando rumo ao MILHÃO" },
];

export default function JourneySection() {
  return (
    <section className="section-padding bg-surface-elevated">
      <div className="max-w-4xl mx-auto text-center">
        <motion.h2
          initial={{ opacity: 0, y: 20 }}
          whileInView={{ opacity: 1, y: 0 }}
          viewport={{ once: true }}
          className="font-display font-black text-2xl md:text-4xl mb-12"
        >
          Conheça sua jornada milionária dentro da{" "}
          <span className="text-gradient-gold">Mentoria VOE</span>
        </motion.h2>

        <div className="grid gap-4 md:grid-cols-2 lg:grid-cols-3">
          {modules.map((m, i) => (
            <motion.div
              key={i}
              initial={{ opacity: 0, y: 20 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              transition={{ delay: i * 0.08 }}
              className="bg-card border border-border rounded-xl p-6 text-left shadow-card hover:border-primary/40 transition-colors"
            >
              <div className="w-10 h-10 rounded-lg bg-gradient-gold flex items-center justify-center mb-4">
                <m.icon className="w-5 h-5 text-primary-foreground" />
              </div>
              <p className="text-primary font-display font-bold text-sm mb-1">{m.title}</p>
              <p className="text-foreground font-display font-semibold text-base">{m.desc}</p>
            </motion.div>
          ))}
        </div>

        <p className="text-muted-foreground mt-8 text-sm max-w-xl mx-auto">
          Todo o conteúdo produzido com qualidade de cinema disponível para você ver e rever quantas vezes for necessário.
        </p>
      </div>
    </section>
  );
}


--------------------------------------------------------------------------------
ARQUIVO: src/components/ForWhoSection.tsx
--------------------------------------------------------------------------------
import { motion } from "framer-motion";
import { Clock, Heart, Briefcase, Wallet, GraduationCap, HelpCircle } from "lucide-react";

const items = [
  { icon: Clock, text: "Trabalhe o dia inteiro e não tenha muito tempo sobrando" },
  { icon: Heart, text: "Tenha que dividir esse tempo com o marido, esposa ou os filhos" },
  { icon: Briefcase, text: "Não queira abandonar o emprego antes de ter uma boa renda" },
  { icon: Wallet, text: "Não tenha muito dinheiro para investir em um negócio digital" },
  { icon: GraduationCap, text: "Não tenha feito uma faculdade" },
  { icon: HelpCircle, text: "Ainda não saiba absolutamente nada sobre Dropshipping" },
];

export default function ForWhoSection() {
  return (
    <section className="section-padding bg-background">
      <div className="max-w-3xl mx-auto text-center">
        <motion.h2
          initial={{ opacity: 0, y: 20 }}
          whileInView={{ opacity: 1, y: 0 }}
          viewport={{ once: true }}
          className="font-display font-black text-2xl md:text-4xl mb-12"
        >
          O Plano da VOE forma milionários,{" "}
          <span className="text-gradient-gold">mesmo que você:</span>
        </motion.h2>

        <div className="grid gap-4 sm:grid-cols-2">
          {items.map((item, i) => (
            <motion.div
              key={i}
              initial={{ opacity: 0, y: 15 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              transition={{ delay: i * 0.08 }}
              className="flex items-start gap-3 bg-card border border-border rounded-xl p-5 text-left shadow-card"
            >
              <item.icon className="w-6 h-6 text-primary flex-shrink-0 mt-0.5" />
              <p className="text-foreground text-sm md:text-base">{item.text}</p>
            </motion.div>
          ))}
        </div>

        <motion.div
          initial={{ opacity: 0, y: 15 }}
          whileInView={{ opacity: 1, y: 0 }}
          viewport={{ once: true }}
          transition={{ delay: 0.5 }}
          className="mt-10"
        >
          <a href="#pricing" className="btn-cta">
            Quero acelerar com a Mentoria VOE
          </a>
          <p className="text-sm text-muted-foreground mt-3">
            Lote Promocional com vagas limitadas
          </p>
        </motion.div>
      </div>
    </section>
  );
}


--------------------------------------------------------------------------------
ARQUIVO: src/components/PricingSection.tsx
--------------------------------------------------------------------------------
import { motion } from "framer-motion";
import { CreditCard, QrCode, CheckCircle } from "lucide-react";

export default function PricingSection() {
  return (
    <section id="pricing" className="section-padding bg-background relative overflow-hidden">
      <div className="absolute top-0 left-1/2 -translate-x-1/2 w-[500px] h-[300px] rounded-full opacity-10 blur-[100px]" style={{ background: "hsl(42 100% 50%)" }} />

      <div className="relative z-10 max-w-3xl mx-auto text-center">
        <motion.h2
          initial={{ opacity: 0, y: 20 }}
          whileInView={{ opacity: 1, y: 0 }}
          viewport={{ once: true }}
          className="font-display font-black text-2xl md:text-4xl mb-4"
        >
          QUANTO VALE A SUA{" "}
          <span className="text-gradient-gold">LIBERDADE FINANCEIRA?</span>
        </motion.h2>

        <motion.div
          initial={{ opacity: 0, y: 20 }}
          whileInView={{ opacity: 1, y: 0 }}
          viewport={{ once: true }}
          transition={{ delay: 0.15 }}
          className="bg-card border border-border rounded-2xl p-6 md:p-10 shadow-card mt-10"
        >
          <div className="space-y-3 text-left mb-8">
            {[
              ["Valor Total dos Cursos Por +5 Anos", "R$ 24.850,00"],
              ["Valor Total dos Bônus", "R$ 7.300,00"],
            ].map(([label, value]) => (
              <div key={label} className="flex justify-between items-center border-b border-border pb-3">
                <span className="text-muted-foreground text-sm md:text-base">{label}</span>
                <span className="font-display font-bold text-foreground">{value}</span>
              </div>
            ))}
          </div>

          <div className="mb-2">
            <p className="text-muted-foreground line-through text-lg">De: R$ 32.150,00</p>
            <p className="text-muted-foreground line-through text-base">Por: R$ 2.997,00</p>
          </div>

          <div className="my-6 py-4 px-6 bg-surface-elevated rounded-xl border border-primary/30">
            <p className="text-sm text-primary font-display font-semibold mb-1">
              R$1.500 DE DESCONTO — ANIVERSÁRIO DA VOE
            </p>
            <p className="font-display font-black text-4xl md:text-5xl text-gradient-gold">
              12x de R$149,70
            </p>
            <p className="text-muted-foreground mt-1">OU R$1.497 À VISTA</p>
          </div>

          <p className="text-sm text-muted-foreground mb-6">
            Se você já é aluno de qualquer curso do Gustavo, terá um desconto adicional exclusivo. É só preencher seus dados no momento da compra que o cupom será aplicado automaticamente!
          </p>

          <a href="#" className="btn-cta inline-block w-full md:w-auto">
            FAZER INSCRIÇÃO
          </a>

          <div className="flex justify-center gap-4 mt-4 text-muted-foreground">
            <CreditCard className="w-6 h-6" />
            <QrCode className="w-6 h-6" />
          </div>
        </motion.div>
      </div>
    </section>
  );
}


--------------------------------------------------------------------------------
ARQUIVO: src/components/GuaranteeSection.tsx
--------------------------------------------------------------------------------
import { motion } from "framer-motion";
import { ShieldCheck, CheckCircle } from "lucide-react";

export default function GuaranteeSection() {
  return (
    <section className="section-padding bg-surface-elevated">
      <div className="max-w-3xl mx-auto text-center">
        <motion.div
          initial={{ opacity: 0, scale: 0.9 }}
          whileInView={{ opacity: 1, scale: 1 }}
          viewport={{ once: true }}
          className="bg-card border border-border rounded-2xl p-8 md:p-12 shadow-card"
        >
          <ShieldCheck className="w-16 h-16 text-primary mx-auto mb-6" />

          <h2 className="font-display font-black text-2xl md:text-3xl mb-4">
            GARANTIA DE RESULTADO COMPROVADO
          </h2>

          <p className="text-gradient-gold font-display font-bold text-xl md:text-2xl mb-8">
            Ou você vende R$10 mil nos primeiros 90 dias ou leva R$3 mil no PIX
          </p>

          <div className="space-y-3 text-left max-w-xl mx-auto mb-8">
            {[
              "Se você não vender R$10 mil depois de assistir todas as aulas do método",
              "Participar dos encontros ao vivo com o Gustavo",
              "Comprovar que a sua loja esteve ativa durante 90 dias",
              "Devolveremos o DOBRO do seu dinheiro investido sem burocracia",
              "Garantia de resultado comprovado reconhecida em cartório",
            ].map((item, i) => (
              <div key={i} className="flex items-start gap-3">
                <CheckCircle className="w-5 h-5 text-primary flex-shrink-0 mt-0.5" />
                <p className="text-muted-foreground text-sm md:text-base">{item}</p>
              </div>
            ))}
          </div>

          <div className="border-t border-border pt-6 mt-6">
            <h3 className="font-display font-bold text-lg mb-2">Ainda não tem certeza?</h3>
            <p className="text-muted-foreground text-sm">
              Você tem <span className="text-primary font-semibold">7 dias</span> para experimentar a Mentoria VOE. Se por qualquer motivo decidir que não quer continuar, devolvemos 100% do seu investimento sem burocracia.
            </p>
          </div>
        </motion.div>
      </div>
    </section>
  );
}


--------------------------------------------------------------------------------
ARQUIVO: src/components/BioSection.tsx
--------------------------------------------------------------------------------
import { motion } from "framer-motion";

export default function BioSection() {
  return (
    <section className="section-padding bg-background">
      <div className="max-w-4xl mx-auto">
        <motion.div
          initial={{ opacity: 0, y: 20 }}
          whileInView={{ opacity: 1, y: 0 }}
          viewport={{ once: true }}
          className="bg-card border border-border rounded-2xl p-8 md:p-12 shadow-card flex flex-col md:flex-row gap-8 items-center"
        >
          <div className="flex-shrink-0 w-40 h-40 md:w-48 md:h-48 rounded-full bg-gradient-gold flex items-center justify-center shadow-gold">
            <span className="font-display font-black text-4xl text-primary-foreground">GH</span>
          </div>

          <div>
            <h2 className="font-display font-black text-2xl md:text-3xl mb-4 text-gradient-gold">
              GUSTAVO HENRIQUE
            </h2>
            <div className="space-y-3 text-muted-foreground text-sm md:text-base leading-relaxed">
              <p>
                Cansado de viver na prisão da CLT, criou sua primeira loja online sem estoque em 2020 e trabalhava à noite, apenas algumas horas por dia.
              </p>
              <p>
                Em apenas 3 meses, ele bateu mais de 100 mil reais em vendas e pôde finalmente ligar para o seu antigo chefe pedindo demissão.
              </p>
              <p>
                Depois de faturar mais de <span className="text-primary font-semibold">R$2,7 milhões em um único mês</span>, começou a ajudar outras pessoas a também conquistarem a liberdade financeira. O próximo pode ser você.
              </p>
              <p className="text-primary font-display font-bold text-lg">Bora voar junto? 🦅</p>
            </div>
          </div>
        </motion.div>
      </div>
    </section>
  );
}


--------------------------------------------------------------------------------
ARQUIVO: src/components/FAQSection.tsx
--------------------------------------------------------------------------------
import { motion } from "framer-motion";
import {
  Accordion,
  AccordionContent,
  AccordionItem,
  AccordionTrigger,
} from "@/components/ui/accordion";

const faqs = [
  {
    q: "Sou iniciante e não tenho experiência com internet. A mentoria serve para mim?",
    a: "Sem sombra de dúvidas! O conteúdo foi estruturado com nivelamento e tutoriais básicos para que qualquer iniciante se sinta tranquilo para executar, botão por botão.",
  },
  {
    q: "Tenho pouco tempo disponível por conta do trabalho, é possível pra mim?",
    a: "Sim! Desenvolvi a metodologia VOE para que você também possa começar com 1 ou 2 horas vagas, sem prejudicar o seu trabalho atual.",
  },
  {
    q: "Preciso mostrar o meu rosto para ter resultados?",
    a: "Não! Uma das grandes vantagens do dropshipping é que você pode faturar sem precisar aparecer ou criar conteúdo em redes sociais.",
  },
  {
    q: "Quanto de investimento é necessário para começar?",
    a: "Como o dropshipping não necessita de estoque, o investimento inicial é praticamente zero. Você só vai precisar de um domínio próprio (cerca de 5 reais por mês).",
  },
  {
    q: "O que é Dropshipping?",
    a: "É um modelo de negócio onde você possui uma loja online que vende qualquer tipo de produto sem precisar de estoque. O fornecedor faz a gestão de estoque e logística.",
  },
  {
    q: "Quando recebo o acesso às aulas?",
    a: "Assim que a sua inscrição for confirmada, imediatamente você receberá um e-mail com instruções, login e senha para acessar a área de membros.",
  },
  {
    q: "Quais são as formas de pagamento?",
    a: "Você pode comprar à vista no boleto, cartão de crédito ou PIX. Também pode parcelar em até 12x no cartão de crédito.",
  },
  {
    q: "E se eu me arrepender da compra?",
    a: "Você pode solicitar o cancelamento em até 7 dias após a data da compra e o valor total será reembolsado, sem taxas.",
  },
];

export default function FAQSection() {
  return (
    <section className="section-padding bg-background">
      <div className="max-w-3xl mx-auto">
        <motion.h2
          initial={{ opacity: 0, y: 20 }}
          whileInView={{ opacity: 1, y: 0 }}
          viewport={{ once: true }}
          className="font-display font-black text-2xl md:text-4xl text-center mb-12"
        >
          PERGUNTAS <span className="text-gradient-gold">FREQUENTES</span>
        </motion.h2>

        <Accordion type="single" collapsible className="space-y-3">
          {faqs.map((faq, i) => (
            <AccordionItem
              key={i}
              value={`faq-${i}`}
              className="bg-card border border-border rounded-xl px-5 shadow-card"
            >
              <AccordionTrigger className="font-display font-semibold text-sm md:text-base text-left hover:no-underline py-5">
                {faq.q}
              </AccordionTrigger>
              <AccordionContent className="text-muted-foreground text-sm md:text-base pb-5">
                {faq.a}
              </AccordionContent>
            </AccordionItem>
          ))}
        </Accordion>
      </div>
    </section>
  );
}


--------------------------------------------------------------------------------
ARQUIVO: src/components/FooterSection.tsx
--------------------------------------------------------------------------------
export default function FooterSection() {
  return (
    <footer className="py-8 px-4 text-center border-t border-border bg-background">
      <p className="text-muted-foreground text-xs">
        © {new Date().getFullYear()} Mentoria VOE — Gustavo Henrique. Todos os direitos reservados.
      </p>
      <p className="text-muted-foreground text-xs mt-2">
        Este produto não garante a obtenção de resultados. Qualquer referência ao desempenho de uma estratégia não deve ser interpretada como garantia de resultados.
      </p>
    </footer>
  );
}


--------------------------------------------------------------------------------
ARQUIVO: src/components/FloatingCTA.tsx
--------------------------------------------------------------------------------
import { motion } from "framer-motion";

export default function FloatingCTA() {
  return (
    <motion.div
      initial={{ y: 100 }}
      animate={{ y: 0 }}
      transition={{ delay: 2, type: "spring", stiffness: 100 }}
      className="fixed bottom-0 left-0 right-0 z-50 p-3 bg-surface-glass border-t border-border md:hidden"
    >
      <a href="#pricing" className="btn-cta block text-center text-sm w-full">
        GARANTIR MINHA VAGA — 12x R$149,70
      </a>
    </motion.div>
  );
}


--------------------------------------------------------------------------------
ARQUIVO: src/components/NavLink.tsx
--------------------------------------------------------------------------------
import { NavLink as RouterNavLink, NavLinkProps } from "react-router-dom";
import { forwardRef } from "react";
import { cn } from "@/lib/utils";

interface NavLinkCompatProps extends Omit<NavLinkProps, "className"> {
  className?: string;
  activeClassName?: string;
  pendingClassName?: string;
}

const NavLink = forwardRef<HTMLAnchorElement, NavLinkCompatProps>(
  ({ className, activeClassName, pendingClassName, to, ...props }, ref) => {
    return (
      <RouterNavLink
        ref={ref}
        to={to}
        className={({ isActive, isPending }) =>
          cn(className, isActive && activeClassName, isPending && pendingClassName)
        }
        {...props}
      />
    );
  },
);

NavLink.displayName = "NavLink";

export { NavLink };


--------------------------------------------------------------------------------
ARQUIVO: src/lib/utils.ts
--------------------------------------------------------------------------------
import { clsx, type ClassValue } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}

